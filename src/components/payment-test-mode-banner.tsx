/**
 * Lemon Squeezy shows its own "Test mode" ribbon inside the checkout overlay,
 * so no separate banner is needed. Kept as a no-op to avoid touching layouts.
 */
export function PaymentTestModeBanner() {
  return null;
}
